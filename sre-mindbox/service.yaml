apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  # Сервис сделаем таким, чтобы он захватывал все поды с меткой
  type: LoadBalancer
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
